<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vrlease.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail(name, image, dish_id, setmeal_id, dish_flavor, number, amount, order_id)
        values
        <foreach collection="orderDetails" item="orderDetail" separator=",">
            (#{orderDetail.name},#{orderDetail.image},#{orderDetail.dishId},#{orderDetail.setmealId},#{orderDetail.dishFlavor},#{orderDetail.number},#{orderDetail.amount},#{orderDetail.orderId})
        </foreach>
    </insert>
    <select id="list" resultType="com.vrlease.entity.OrderDetail">
        select * from order_detail where order_id = #{id}
    </select>
    <select id="getByOrderId" resultType="com.vrlease.entity.OrderDetail">
        select * from order_detail where order_id = #{id}
    </select>
</mapper>
